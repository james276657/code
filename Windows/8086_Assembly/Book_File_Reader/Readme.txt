Book Reader 8086 assembly.  The masm32 tool set can be installed from
the zip.  The tools are a bit quirky so the code needs to reside on
the c drive and masm32 needs to be in root.  Console builds are used.

Resources can be compiled with a masm32 tool.

